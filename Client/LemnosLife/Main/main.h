#ifndef MAIN_H_INCLUDED
#define MAIN_H_INCLUDED

#include <string>
#include <thread>

extern std::string name, nameLg, nameUp, version, versionFull, ext, dir, assets, styles, picturesFolder, downloads, skins, mapFolder, commonMapFolder, picturesMapFolder, picturesMapGroundFolder, pathSeparatorStr;
extern char pathSeparator;
extern bool DEBUG;
void free();

#endif
