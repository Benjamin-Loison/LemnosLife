#ifndef MATH_PLUS_H_INCLUDED
#define MATH_PLUS_H_INCLUDED

bool isInteger(double);
int absolute(int);
double remainderEuclidianDivision(double, double), remainderEuclidianDivisionStrict(double, double);
long timeIn(double, double);

#endif
